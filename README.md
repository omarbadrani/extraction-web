# extraction-web
A Python-based tool with PyQt5 GUI that extracts personal information (emails, phone numbers, addresses) from websites. It cleans HTML content, detects PII using regex, shows progress in real-time, and generates a structured summary including site description, optimized for Tunisian sites.
